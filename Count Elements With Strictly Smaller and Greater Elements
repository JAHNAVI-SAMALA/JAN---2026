class Solution:
    def countElements(self, nums: List[int]) -> int:
        cnt = 0
        Min = min(nums)
        Max = max(nums)
        for i in nums:
            if Min < i < Max:
                cnt  += 1
        return cnt
