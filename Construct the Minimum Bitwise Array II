class Solution:
    def minBitwiseArray(self, nums: list[int]) -> list[int]:
        res = []
        for x in nums:
            if x == 2:
                res.append(-1)
                continue
            bit = 1
            while x & bit:
                bit <<= 1
            res.append(x ^ (bit >> 1))
        return res
